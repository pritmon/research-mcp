services:
  - type: web
    name: research-mcp
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/src/server.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
    healthCheckPath: /
